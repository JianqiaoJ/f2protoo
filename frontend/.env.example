# 本文件为【模板】，应用不会读取 .env.example，仅作参考。
# 请复制本文件为 .env 或 .env.local，并填写实际值；或在本目录新建 .env / .env.local 后按下面变量名配置。

# 可选：后端 API 地址（默认 http://localhost:3000）
# VITE_API_BASE_URL=http://localhost:3000

# 可选：实验/日志上报地址（如 http://127.0.0.1:7242）。不设置则不会请求，避免控制台出现 ERR_CONNECTION_REFUSED
# VITE_INGEST_URL=http://127.0.0.1:7242

# 可选：OpenRouter API Key（Gemini / GPT / Qwen / Kimi / GPT-4o 等非 DeepSeek 模型）
# 若出现「User not found」「Failed to authenticate request with Clerk」或 401，表示密钥未设置或无效。
# 请到 https://openrouter.ai/keys 获取密钥，写在 .env 或 .env.local 中（不要只改本 .env.example），确保无首尾空格。
# 文档：https://openrouter.ai/docs/quickstart
# VITE_OPENROUTER_API_KEY=sk-or-v1-你的密钥
 VITE_OPENROUTER_API_KEY=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